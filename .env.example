# WAV Automata - Variables de Entorno
# 
# Copia este archivo como `.env` en la raíz del proyecto y completa tus valores reales.
# NUNCA commitees `.env` - solo `.env.example` debe estar en Git.

# ============================================================================
# SUPABASE (Requerido para funcionalidad de persistencia de datos)
# ============================================================================

# URL de tu proyecto Supabase
# Ejemplo: https://abc123def456.supabase.co
SUPABASE_URL=https://your-project.supabase.co

# Clave pública (anon) o clave de servicio de Supabase
# Encontrada en: Supabase Dashboard → Settings → API → Project API keys
# Usa la clave "Service Role" para acceso completo
SUPABASE_KEY=your-supabase-api-key-here

# ============================================================================
# OPENAI (Opcional, para generación mejorada de contenido)
# ============================================================================

# Tu clave de API de OpenAI (si quieres usar GPT para copy mejorado)
# Encontrada en: https://platform.openai.com/api-keys
# Si no la configuras, el sistema usa heurísticas simples
OPENAI_API_KEY=sk-your-openai-key-here

# ============================================================================
# CONFIGURACIÓN DE APLICACIÓN (Opcional)
# ============================================================================

# Nombre del proyecto (mostrado en /health)
PROJECT_NAME=WAV_Automata

# Nivel de logging (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# ============================================================================
# SEGURIDAD (Opcional, requerido para endpoints protegidos)
# ============================================================================

# Token para validar acceso a endpoints administrativos (ej. POST /scheduler/weights/update)
# Usa una cadena aleatoria segura (mínimo 32 caracteres)
# Generador: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
ADMIN_TOKEN=your-admin-token-here

# ============================================================================
# DESARROLLO LOCAL (Opcional)
# ============================================================================

# Establece a true para modo debug
DEBUG=false

# Puerto (por defecto 8000, configurable con argumento de uvicorn)
PORT=8000
